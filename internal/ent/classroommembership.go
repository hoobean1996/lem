// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"gigaboo.io/lem/internal/ent/classroom"
	"gigaboo.io/lem/internal/ent/classroommembership"
	"gigaboo.io/lem/internal/ent/user"
)

// ClassroomMembership is the model entity for the ClassroomMembership schema.
type ClassroomMembership struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Status holds the value of the "status" field.
	Status classroommembership.Status `json:"status,omitempty"`
	// JoinedAt holds the value of the "joined_at" field.
	JoinedAt time.Time `json:"joined_at,omitempty"`
	// LeftAt holds the value of the "left_at" field.
	LeftAt *time.Time `json:"left_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the ClassroomMembershipQuery when eager-loading is set.
	Edges                      ClassroomMembershipEdges `json:"edges"`
	classroom_memberships      *int
	user_classroom_memberships *int
	selectValues               sql.SelectValues
}

// ClassroomMembershipEdges holds the relations/edges for other nodes in the graph.
type ClassroomMembershipEdges struct {
	// Classroom holds the value of the classroom edge.
	Classroom *Classroom `json:"classroom,omitempty"`
	// Student holds the value of the student edge.
	Student *User `json:"student,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// ClassroomOrErr returns the Classroom value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ClassroomMembershipEdges) ClassroomOrErr() (*Classroom, error) {
	if e.Classroom != nil {
		return e.Classroom, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: classroom.Label}
	}
	return nil, &NotLoadedError{edge: "classroom"}
}

// StudentOrErr returns the Student value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e ClassroomMembershipEdges) StudentOrErr() (*User, error) {
	if e.Student != nil {
		return e.Student, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "student"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ClassroomMembership) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case classroommembership.FieldID:
			values[i] = new(sql.NullInt64)
		case classroommembership.FieldStatus:
			values[i] = new(sql.NullString)
		case classroommembership.FieldJoinedAt, classroommembership.FieldLeftAt:
			values[i] = new(sql.NullTime)
		case classroommembership.ForeignKeys[0]: // classroom_memberships
			values[i] = new(sql.NullInt64)
		case classroommembership.ForeignKeys[1]: // user_classroom_memberships
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ClassroomMembership fields.
func (_m *ClassroomMembership) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case classroommembership.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case classroommembership.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = classroommembership.Status(value.String)
			}
		case classroommembership.FieldJoinedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field joined_at", values[i])
			} else if value.Valid {
				_m.JoinedAt = value.Time
			}
		case classroommembership.FieldLeftAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field left_at", values[i])
			} else if value.Valid {
				_m.LeftAt = new(time.Time)
				*_m.LeftAt = value.Time
			}
		case classroommembership.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field classroom_memberships", value)
			} else if value.Valid {
				_m.classroom_memberships = new(int)
				*_m.classroom_memberships = int(value.Int64)
			}
		case classroommembership.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_classroom_memberships", value)
			} else if value.Valid {
				_m.user_classroom_memberships = new(int)
				*_m.user_classroom_memberships = int(value.Int64)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ClassroomMembership.
// This includes values selected through modifiers, order, etc.
func (_m *ClassroomMembership) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryClassroom queries the "classroom" edge of the ClassroomMembership entity.
func (_m *ClassroomMembership) QueryClassroom() *ClassroomQuery {
	return NewClassroomMembershipClient(_m.config).QueryClassroom(_m)
}

// QueryStudent queries the "student" edge of the ClassroomMembership entity.
func (_m *ClassroomMembership) QueryStudent() *UserQuery {
	return NewClassroomMembershipClient(_m.config).QueryStudent(_m)
}

// Update returns a builder for updating this ClassroomMembership.
// Note that you need to call ClassroomMembership.Unwrap() before calling this method if this ClassroomMembership
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ClassroomMembership) Update() *ClassroomMembershipUpdateOne {
	return NewClassroomMembershipClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ClassroomMembership entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ClassroomMembership) Unwrap() *ClassroomMembership {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ClassroomMembership is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ClassroomMembership) String() string {
	var builder strings.Builder
	builder.WriteString("ClassroomMembership(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("status=")
	builder.WriteString(fmt.Sprintf("%v", _m.Status))
	builder.WriteString(", ")
	builder.WriteString("joined_at=")
	builder.WriteString(_m.JoinedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	if v := _m.LeftAt; v != nil {
		builder.WriteString("left_at=")
		builder.WriteString(v.Format(time.ANSIC))
	}
	builder.WriteByte(')')
	return builder.String()
}

// ClassroomMemberships is a parsable slice of ClassroomMembership.
type ClassroomMemberships []*ClassroomMembership
